#sets is the collection of well define objects
s={1,2,3}
print(s)

#For make a empty set
e = set()
print(type(e))

#not get the repeated values
# sets are unordered, un-indexed
#no way to change items in sets